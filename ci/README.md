# Construction CI Commands

The repository is split into backend and frontend validation commands from Phase 1.

Backend command:

```bash
npm run api:test
```

Frontend command:

```bash
npm run mfe:build
```

GitHub Actions can be added after the publishing credential has `workflow` scope.